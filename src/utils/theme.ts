import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#729f03",
      light: "#78cd51",
      dark: "#78cd51",
      contrastText: "#fff",
    },
    background: {
      default: "#F5F5F5",
    },
  },
});

export default theme;
