import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#78cd51",
      light: "#78cd51",
      dark: "#78cd51",
      contrastText: "#fff",
    },
  },
});

export default theme;
