import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#47C53E",
      light: "#47C53E",
      dark: "#47C53E",
      contrastText: "#fff",
    },
    background: {
      default: "#F5F5F5",
    },
  },
});

export default theme;
